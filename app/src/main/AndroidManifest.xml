<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project2_rev1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project2_rev1">
        <activity
            android:name=".menus.EndGame"
            android:exported="false" />
        <activity
            android:name=".AdventureView"
            android:exported="false" />
        <activity
            android:name=".menus.StartGame"
            android:exported="false" />
        <activity
            android:name=".menus.MainMenu"
            android:exported="false" />
        <activity
            android:name=".menus.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>